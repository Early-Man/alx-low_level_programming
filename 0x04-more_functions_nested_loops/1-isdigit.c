#include "main.h"

/**
 * _isdigit - checks for digits 0 to 9.
 * @n: variable to be checked
 * Return: 1 if n is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	if (n >= '0' && n <= '9')
		return (1);
	return (0);
}
