#include "main.h"

/**
 * mul - multiplies two integers
 * @x: the first number to multiply
 * @y: the second number to multiply with first
 * Return: sum
 */
int mul(int x, int y)
{
	int x;
	int y;
	int sum;

	sum = x * y;
	return (sum);
}
